version: "2"
jobs:
  build:
    docker:
      - image: circleci/node:8.1.2
    working_directory: "~/testcafe"
    steps:
      - checkout
      - setup_remote_docker  # ci中でdockerとdocker-composeを使う為の魔法のワード
      - run: docker-compose build
      - run: docker-compose run testcafe